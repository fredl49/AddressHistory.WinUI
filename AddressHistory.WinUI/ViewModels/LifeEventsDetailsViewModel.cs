using System.Collections.ObjectModel;

using AddressHistory.WinUI.Contracts.ViewModels;
using AddressHistory.WinUI.Core.Contracts.Services;
using AddressHistory.WinUI.Core.Models;

using CommunityToolkit.Mvvm.ComponentModel;

namespace AddressHistory.WinUI.ViewModels;

public partial class LifeEventsDetailsViewModel : ObservableRecipient, INavigationAware
{
    private readonly ISampleDataService _sampleDataService;

    [ObservableProperty]
    private SampleOrder? selected;

    public ObservableCollection<SampleOrder> SampleItems { get; private set; } = new ObservableCollection<SampleOrder>();

    public LifeEventsDetailsViewModel(ISampleDataService sampleDataService)
    {
        _sampleDataService = sampleDataService;
    }

    public async void OnNavigatedTo(object parameter)
    {
        SampleItems.Clear();

        // TODO: Replace with real data.
        var data = await _sampleDataService.GetListDetailsDataAsync();

        foreach (var item in data)
        {
            SampleItems.Add(item);
        }
    }

    public void OnNavigatedFrom()
    {
    }

    public void EnsureItemSelected()
    {
        Selected ??= SampleItems.First();
    }
}
